public static Node insertionAtTail(Node head, int data){
     Node new = new Node(data);
     if(head==null) return new;
     Node temp=head;
     while(temp.next!=null){
     temp=temp.next;
     }
     temp.next=new;
     return head;
}